/**
 * DE-6 moved the assistant into Intelligence → Satış Asistanı.
 * Kept as a compatibility component because App.tsx still mounts it globally
 * until the shell cleanup is folded into a later UI pass.
 */
export default function AiChatWidget() {
  return null;
}
